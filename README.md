# ImageDataUtil.js

- utilitiy functions for ImageData

## functions

- decode(bin) : ImageData
- encode(imgdata) : bin
- resize(imgdata, wdst, hdst = 0) : imgdata
- crop(imgdata, x, y, w, h) : imgdata
